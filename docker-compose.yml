version: "3.8"

services:
  rasa:
    build: .
    ports:
      - "5005:5005"
    volumes:
      - .:/app
    command:
      - run
      - --enable-api
      - --cors
      - "*"

  action_server:
    image: rasa/rasa-sdk:3.6.16
    ports:
      - "5055:5055"
    volumes:
      - ./actions:/app/actions